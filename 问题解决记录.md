# 问题解决记录

> 记录项目开发和运行过程中遇到的问题、原因分析及解决方案，便于后续问题排查和知识积累。

---

## 目录

| 序号 | 问题标题 | 发现日期 | 状态 |
|------|----------|----------|------|
| 1 | AI服务恢复后仍返回错误信息 | 2026-04-28 | ✅ 已解决 |

---

## 问题详情

---

### 问题 1：AI服务恢复后仍返回错误信息

#### 📋 问题描述

**现象**：Python服务因网络问题暂时不可用，返回错误信息"AI服务暂时不可用"。服务恢复后，用户继续提问时：
1. 回答仍然显示"AI服务暂时不可用"
2. 返回速度很快（毫秒级）
3. 即使切换新会话也可能出现相同问题

**影响范围**：所有用户对话功能

**严重程度**：高

---

#### 🔍 原因分析

经排查，发现**两个叠加的Bug**：

##### Bug 1：对话历史污染
- 当服务不可用时，错误信息"AI服务暂时不可用"被保存到对话历史中
- 后续问题携带这个历史上下文发送给LLM
- LLM看到对话历史中有"服务不可用"的记录，就顺着这个思路回答

##### Bug 2：Redis缓存污染
- 错误响应被缓存到Redis中（缓存键格式：`ai:answer:{userId}:{question}`）
- 服务恢复后，相同问题直接读取缓存的错误回答
- 导致即使服务恢复，用户仍然看到错误信息

**问题流程图**：
```
用户提问 → Redis缓存命中 → 返回缓存的错误回答
       ↓
（服务已恢复，但缓存未更新）
```

---

#### 🛠️ 解决方案

##### 方案概述
采用"双重防护"策略：
1. **Python端**：过滤对话历史中的错误信息，防止LLM被误导
2. **Java端**：改进缓存策略，不缓存/不使用错误响应

##### 具体修复

**1. Python端修复（`python-service/core/llm.py`）**

新增对话历史清理函数：
```python
def clean_conversation_context(self, context: str) -> str:
    """清理对话上下文，移除错误信息"""
    error_keywords = [
        "AI服务暂时不可用",
        "服务不可用", 
        "系统错误",
        "无法连接",
        "网络错误",
        "超时",
        "API密钥",
        "配置错误"
    ]
    # 过滤包含错误关键词的行
    lines = context.split("\n")
    cleaned_lines = [line for line in lines 
                    if not any(keyword in line for keyword in error_keywords)]
    return "\n".join(cleaned_lines)
```

更新Prompt模板，添加规则：
```python
self.prompt = PromptTemplate.from_template("""
重要规则：
4. 不要提及"AI服务不可用"、"系统错误"等技术问题，你始终处于正常工作状态
...
""")
```

**2. Java端修复（`AiServiceImpl.java`）**

新增错误响应检测：
```java
private boolean isErrorResponse(String answer) {
    String[] errorKeywords = {
        "AI服务暂时不可用",
        "服务不可用",
        "系统错误",
        "无法连接",
        "网络错误",
        "暂时无法回答",
        "稍后再试"
    };
    for (String keyword : errorKeywords) {
        if (answer.contains(keyword)) {
            return true;
        }
    }
    return false;
}
```

新增友好错误提示语：
```java
private String getFriendlyErrorMessage(String errorType) {
    String[] friendlyMessages = {
        "正在努力处理您的问题，请稍等片刻...",
        "系统正在维护中，很快就会恢复，请稍后再试",
        "服务暂时繁忙，请稍后重试",
        "我正在连接知识库，请耐心等待",
        "让我想想，马上为您解答"
    };
    int index = Math.abs(errorType.hashCode()) % friendlyMessages.length;
    return friendlyMessages[index];
}
```

修改缓存逻辑：
```java
// 缓存命中时：如果是错误响应，不使用缓存
if (cachedResponse != null) {
    if (isErrorResponse(cachedResponse.getAnswer())) {
        log.info("Cache hit but contains error response, refreshing...");
        // 不使用缓存，继续执行请求
    } else {
        return cachedResponse;
    }
}

// 缓存写入时：错误响应不缓存
if (!isErrorResponse(answer)) {
    cacheService.set(CacheConfig.CACHE_AI_ANSWER, cacheKey, aiResponse);
}
```

---

#### ✅ 修复验证

| 验证场景 | 预期结果 | 实际结果 |
|----------|----------|----------|
| 服务恢复后问相同问题 | 返回正确回答 | ✅ 通过 |
| 对话历史有错误记录 | LLM正常回答 | ✅ 通过 |
| 错误信息显示 | 显示友好提示语 | ✅ 通过 |
| 错误响应缓存 | 不被缓存 | ✅ 通过 |

---

#### 📌 预防措施

1. **缓存策略优化**：错误响应不应被缓存，设置较短的缓存时间
2. **监控告警**：添加服务健康检查和告警机制
3. **降级策略**：服务不可用时返回友好提示而非技术错误信息
4. **日志记录**：完善错误日志，便于问题定位
5. **定期清理**：定时清理过期或错误的缓存数据

---

#### 📝 备注

- **修复版本**：Python服务 v1.1.0 / Java服务 v1.0.5
- **修复人**：[开发者名称]
- **相关文件**：
  - `python-service/core/llm.py`
  - `src/main/java/com/demo/aiknowledge/service/impl/AiServiceImpl.java`

---

## 附录

### 问题分类

| 分类 | 说明 | 示例 |
|------|------|------|
| 🔴 严重 | 系统崩溃、数据丢失、安全漏洞 | 服务无法启动 |
| 🟠 高 | 核心功能不可用、影响大量用户 | 问答功能异常 |
| 🟡 中 | 部分功能异常、用户体验下降 | 界面显示错误 |
| 🟢 低 | 轻微问题、不影响主要功能 | 日志警告 |

### 状态标识

| 标识 | 含义 |
|------|------|
| 📋 | 待分析 |
| 🔍 | 分析中 |
| 🛠️ | 修复中 |
| ✅ | 已解决 |
| ⏳ | 等待验证 |
| ⚠️ | 复现困难 |

---

*文档创建时间：2026-04-28*  
*最后更新时间：2026-04-28*